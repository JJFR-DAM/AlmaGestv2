import 'package:flutter/material.dart';

class OpinionScreen extends StatelessWidget {
  const OpinionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('OpinionScreen'),
      ),
    );
  }
}
